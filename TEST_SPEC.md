# TEST_SPEC.md — hodo-hangenki

<!-- scaffold template v1.8.0 から展開(2026-08-19)。本プロジェクトは R のため実行規約を testthat に読み替える -->

## 実行規約

- テスト実行と `test_run` 記録は `python harness/testrun.py --loop <loop_id>` 経由を必須とする(HC-003 直移植)
- テストは 3 層: `unit`(KM・寿命導出の純関数)/ `integration`(収集→計算→レンダリング)/ `validation`(理論・独立実装オラクル)
- オラクルテストを書く前に、(a) 照合対象の実出力のスキーマを読み、(b) 期待値の導出前提を
  テスト内 assert で検算し、導出過程をコメントに残す(HC-001 直移植)
- G-02 の許容誤差は較正実験(合成データの n とばらつきの実測)を経てから固定する。
  実験前に推測で閾値を書かない
- 照合・パースを含むテストは、書く前に**特殊値の挙動表**(NA/NaN/0/端点/形式方言に
  ついて両側がどう振る舞うか)を作り、除外規則と共にテスト内コメントへ残す。
  時刻のパースは必ず format/tz を明示する(HC-001)

## オラクルの構造

「外部の正解」は公表統計ではなく**理論と独立実装**の 2 本柱:

1. **閉形式** — 指数分布の S(t)=exp(−λt)、中央値 ln2/λ(kobai-walk 型)
2. **独立実装** — survival::survfit との数値一致(tegaki-yomi 型)
3. **構造不変量** — append-only 台帳・打ち切りの単調性・S(t) の非増加性
4. **決定論回帰** — SVG ハッシュ(toukei-atlas 型)

## ケース一覧

| ID | 対応要求 | 層 | ケース | 期待 |
|---|---|---|---|---|
| T-001 | G-01/F-03 | unit | 手計算フィクスチャ(6 件、打ち切り 2 件)の KM 階段関数 | 各イベント時刻の S(t) が手計算値と厳密一致(導出コメント必須) |
| T-002 | G-01/F-04 | unit | 同フィクスチャの中央生存時間 | 手計算値と厳密一致 |
| T-003 | F-03 | unit | S(t) の構造 | 非増加・S(0)=1・イベント時刻でのみ変化 |
| T-004 | G-05 | validation | 自前 KM vs survival::survfit(合成データ数種) | 階段関数・中央値・Greenwood SE が数値一致(tolerance 1e-12 目標、実測で固定) |
| T-005 | G-02 | validation | 指数分布合成データ(λ・シード固定) | S(t) と exp(−λt) の最大乖離・中央値と ln2/λ の乖離が較正済み許容誤差内 |
| T-006 | G-06/F-02 | unit | 1 回だけ観測 → 直後に消えたストーリー | time = スナップショット間隔の定義どおり、event=1 |
| T-007 | G-06/F-02 | unit | 最新スナップショットに存在するストーリー | event=0(右打ち切り)、time = 経過時間 |
| T-008 | G-06/F-02 | unit | 収集欠損(間隔 2 倍)を跨いで生存 | 欠損を「消えた」と誤判定しない(定義は実装時に固定しテストに写す) |
| T-009 | G-03/F-01 | integration | data/ledger.csv | 全スナップショットのファイル SHA256 が台帳と一致(append-only) |
| T-010 | G-03 | integration | スナップショット内の guid | 重複 0・収集時刻がファイル名と一致・時系列単調 |
| T-011 | F-01 | unit | RSS パーサ(保存済みフィクスチャ XML) | guid/link/title/category が期待どおり。実フィードへのネットワークアクセスをテストで行わない |
| T-012 | G-04/F-08 | integration | KM 曲線 SVG の 2 回レンダリング | バイト一致 + 回帰フィクスチャとハッシュ一致 |
| T-013 | F-05 | integration | out/ の構成 | index + カテゴリ別ページに定型 4 ブロック + フッタ、`<script` なし |
| T-014 | F-06 | unit | フッタ定義 | フリート標準の順序・全 https・プレースホルダなし |
| T-015 | F-04 | validation | Greenwood 信頼区間 | survfit の conf.int と一致(log 変換の流儀を実測で確認してから固定) |

註: 実フィードの内容は変動するため、収集系のテストはすべて**保存済みフィクスチャ**に
対して行う。ネットワークは 01_collect 実行時のみ。
