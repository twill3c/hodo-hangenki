# SPEC.md — hodo-hangenki(報道半減期)

<!-- scaffold template v1.8.0 から展開(2026-08-19)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

ニュース見出しを「読む」のではなく「測る」観測所。公開 RSS を定時スナップショットし、
各ニュースが見出し欄に留まった時間を**生存時間解析**(カプラン・マイヤー推定)にかけて、
カテゴリ別の**半減期**(生存曲線の中央値)を静的サイトとして公開する。
R の看板芸(survival)をニュースに適用した、フリート初の「自動更新される静的サイト」。

## 2. ドメインモデル

- **ストーリー** = RSS item(同一性は guid、無ければ link で識別)
- **スナップショット** = 時刻 t にフィードへ載っていたストーリー集合(1 回の収集 = 1 ファイル、不変)
- **滞在時間** = first_seen から last_seen までの経過時間。**最新スナップショットに
  まだ載っているストーリーは右打ち切り**(censored)として扱う — ここが KM 推定を使う理由
- **半減期** = KM 生存曲線 S(t) が 0.5 を下回る最小の t(= 生存時間の中央値)

## 3. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 収集: NHK RSS 8 フィード(loop_001 で pin: `https://news.web.nhk/n-data/conf/na/rss/cat{0..7}.xml`、cat0 主要/1 社会/2 暮らし/3 科学・文化/4 政治/5 経済/6 国際/7 スポーツ。旧 www.nhk.or.jp/rss は 301 のため最終 URL を使用)を取得し、`data/snapshots/` に 1 収集 = 1 CSV(UTC タイムスタンプ名・**不変**)で追記する。保存項目は guid・link・title・category・収集時刻のみ(本文は保存しない)。同一 (guid, category) はスナップショット内で一意(主要と各論カテゴリの併載は別行として許す) | must |
| F-02 | 寿命計算: スナップショット列からストーリーごとの (time, event) を導出する。event=1(消えた)/ 0(右打ち切り)。収集欠損(スナップショット間隔の乱れ)に頑健な定義を持つ | must |
| F-03 | KM 推定: **自前の純関数実装**で S(t) を計算する(survival パッケージは G-05 の照合相手であり本線依存にしない) | must |
| F-04 | 半減期の算出: カテゴリ別の中央生存時間 + 95% 信頼区間(Greenwood) | must |
| F-05 | ページ: index(全カテゴリの KM 曲線重ね描き + 半減期一覧表)+ カテゴリ別ページ(定型 4 ブロック: KM 曲線(主役)+ 長寿/短命ストーリー各 10 + 滞在時間ヒストグラム + 出典・定義注記) | must |
| F-06 | フッタ: フリート標準 5 リンク(License/GitHub/歩き方/設計図/App Menu)。アーティファクト公開までは 3 リンク | must |
| F-07 | 自動更新: GitHub Actions cron が収集 → 再計算 → 再レンダリング → コミット → デプロイまでを無人実行する | must |
| F-08 | 静的・閲覧時 JS なし(toukei-atlas と同方針)。SVG は svglite 決定論レンダリング | must |

## 4. 品質基準(ゲート)

`python harness/testrun.py --loop <id>`(testthat)が green であること。

| ゲート | 基準 |
|---|---|
| G-01 KM 正確性(厳密) | 手計算フィクスチャ(打ち切り込み 6 件程度)で KM の階段関数値が**厳密一致**。導出過程はテスト内コメントに残す |
| G-02 KM 正確性(統計) | 合成データ(指数分布 λ 既知・一様打ち切り、シード固定)で S(t) が閉形式 exp(−λt) と許容誤差内、中央生存時間が ln2/λ と許容誤差内。**許容誤差は較正実験で決めてから固定する**(HC-001 系の教訓: 閾値を先に推測しない) |
| G-05 二実装照合 | 自前 KM 実装と `survival::survfit` の階段関数・中央値・Greenwood SE が数値一致(tegaki-yomi 型オラクル) |
| G-03 台帳整合 | スナップショットは append-only: 既存ファイルのハッシュ台帳(`data/ledger.csv`)と全件一致。タイムスタンプ単調、guid の同一スナップショット内重複 0 |
| G-04 描画決定論 | 同一入力 → SVG バイト一致 + 回帰フィクスチャ照合(toukei-atlas 直移植) |
| G-06 寿命導出の境界 | 1 回しか観測されないストーリー、収集欠損跨ぎ、最新スナップショット新規、の 3 境界ケースの (time, event) をフィクスチャで固定 |

## 5. 運用・法務上の約束

- 見出しとリンクのみ保存・表示し、本文・画像は取得も保存もしない。全ページに出典
  (NHK ほか)とフィード取得時刻を明記し、各見出しは配信元へのリンクにする
- 配信元がフィード提供を止めた・利用条件を変えた場合は、当該ソースの収集を止める
- User-Agent にプロジェクト URL を入れ、収集間隔は 1 時間より密にしない

## 6. スコープ外

- 本文の取得・全文検索・要約(見出しメタデータのみ)
- 感情分析・フレーミング比較(案 2 の領域。本作は寿命に集中)
- リアルタイム表示(cron 周期での更新のみ)
- e-Stat 型の公表値照合 — 本作の「外部の正解」は理論(閉形式)と独立実装(survival)である

## 7. アーキテクチャ方針

- toukei-atlas の資産を直移植: glue テンプレート静的生成・svglite 決定論・pins 型の台帳思想・
  HC-001〜003 の規律(オラクルは実データ検分後に固定 / ネイティブ経路はスモーク / testrun.py)
- `R/` は純関数のみ(IO・時刻・乱数禁止)。収集時刻は collect スクリプトが引数注入
- デプロイ: GitHub Actions が out/ をコミットし、Vercel の Git 連携(kyoka-grid 方式)で
  静的配信する(Vercel 上でビルドしない)
