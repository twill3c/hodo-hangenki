# data/processed/ から index + カテゴリ別ページ(定型 4 ブロック)を out/ に生成する。
#
# 契約(loop_003 で実装、テスト T-012〜T-014 が先行):
#  - KM 曲線(主役)+ 長寿/短命ストーリー各 10 + 滞在時間ヒストグラム + 出典・定義注記
#  - svglite 決定論レンダリング(G-04)。テンプレートは site/(glue 展開)
#  - フッタは R/footer.R(フリート標準)
#  - 収集時刻・生成時刻は本スクリプトが引数注入(R/ 内で Sys.time() を呼ばない)

stop("loop_003 で実装する(テスト先行)。SPEC §3 F-05〜F-06 参照")
